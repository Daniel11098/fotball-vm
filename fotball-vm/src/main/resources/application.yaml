spring:
  application:
    name: fotball-vm
  datasource:
    url: jdbc:postgresql://localhost:5432/fotball_vm
    username: postgres
    password: Valle&Hovin1143
  driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
api-football:
  key: ${API_FOOTBALL_KEY}
  base-url: https://v3.football.api-sports.io

